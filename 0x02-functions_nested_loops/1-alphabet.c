#include "main.h"

/**
 * main - entry point
 * description: this func print the alphabet in lower case
 */

void print_alphabet(void)
{
char i;
for (i = 'a'; i <= 'z'; ++i)
{
_putchar(i);
}
_putchar(10);
}
