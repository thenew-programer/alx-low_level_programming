#include <stdio.h>

/**
 * main - this program prints the number of
 * arguments passed into it
 * Return: 0 in Success
 */

int main(int argc, char **argv)
{
    printf("%d\n", (argc - 1));
    return (0);
}

