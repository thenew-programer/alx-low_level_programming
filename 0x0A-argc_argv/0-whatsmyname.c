#include <stdio.h>

/**
 * main - Entry point
 * Description: Program that prints its name followed by a new line
 * Return: 0
 */

int main(int argc, char **argv)
{
    printf("%s\n", argv[0]);
    return (0);
}
